"""Question bank: seed loading + random selection.

Step-1 stub. Full implementation arrives in step 2 with real seed data.
"""
import logging

log = logging.getLogger(__name__)


def seed_if_empty() -> None:
    """Load seed JSON files into the questions table if it's currently empty.

    Implemented in step 2 once the seed_data JSON files exist.
    """
    log.info("seed_if_empty: skipped (step-2 placeholder)")
